
# Dijkstra algorithm on a graph defined as a dictionary with id strings as key
# and a list of tuple of id strings and distance float.
# The dictionary is a simple implementation of an adjacency list.
def dijkstra(g : dict[str, list[tuple[str, int]]]):
    pass